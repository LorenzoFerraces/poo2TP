package container;

public class ContainerTanque extends Container {
	double kwHora;

	public ContainerTanque(int ancho, int largo, int altura, int pesoTotal) {
		super(ancho, largo, altura, pesoTotal);
	}
}
